// Ambos os métodos fazem a mesma quantia de comparações, a diferença é que o bubble faz mais trocas que o selection, já que o bubble troca os elementos assim que 
// encontra um em ordem incorreta, enquanto o selection só faz a troca quando encontra o elemento correto para a posição atual.
